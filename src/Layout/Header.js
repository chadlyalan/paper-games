import { AppBar } from '@material-ui/core';
import React from 'react';

class Header extends React.Component {
    render () {
        return (
            //this is where our header is going to go
        <AppBar className="navBar">Paper Games</AppBar>
        )
    }
}

export default Header;